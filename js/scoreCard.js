(function(window, comp, $, undefined) {
    "use strict";

    function ScoreCard(params) {

    }

    ScoreCard.prototype = {
        constructor: ScoreCard
    };

    comp.ScoreCard = ScoreCard;
})(window, CK12.components, jQuery);